# GaussDB 性能诊断

记录 SQL、版本、表规模、分布/分区、统计信息时间、计划与实际耗时。比较计划估计行数和实际行数；差异大时先确认统计信息，而不是立即改 SQL。

关注 Scan、Join、Aggregate、Sort 和节点间数据移动。大表 JOIN 的分布键是否一致、是否出现倾斜、JOIN 是否放大数据量；过滤条件能否裁剪分区；聚合和排序是否溢出。列存与行存选择结合访问模式。

ANALYZE、VACUUM、索引及分布键变更都应先确认版本支持、操作窗口和影响范围。没有执行计划或基数证据时给出排查方向，不断言根因。
